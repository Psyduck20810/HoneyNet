version: "3.9"

services:
  honeypot:
    build: .
    ports:
      - "80:5000"
    environment:
      - MONGO_URI=${MONGO_URI}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
    volumes:
      - ./logs:/honeypot/logs
    restart: unless-stopped
